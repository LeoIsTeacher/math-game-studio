# 回传验收、版本与组件沉淀

## 回传验收

主控收到摘要后先对照任务书，再检查成果。摘要中的“完成”不等于主控验收通过。

- **教学与玩法**：数学目标能否被观察；学生操作与目标是否对应；正确/错误/边界示例能否按规则解释。通过后只批准原型工作，不宣称原型已验证。
- **原型**：入口能运行；核心循环可完成；Leo 是否实际试玩并确认玩法。若仅代码完成而尚无试玩，状态保持待验证。
- **美术**：角色设定是否先确认；形象、比例和状态是否一致；数量/数值可读；实际文件是否存在且符合接入规格。
- **动画**：序列规格和锚点是否齐全；状态映射是否明确；重复/中断/重启能否恢复；未获得真实动画时不把分镜当成成品。
- **音频**：是否有实际可用资源、清晰原创方向与来源状态；循环和事件映射是否可用；无声也能理解反馈；只有提示词时记待制作。
- **开发**：是否仅改约定范围；相关锁定规则是否保持；新资源与程序能否共同运行；有无本次相关的运行证据。
- **测试**：分开程序检查、教师试玩、学生试玩；结论是否有对应的版本和真实记录；未执行项不可勾为通过。

接受部分成果时逐项记录“已接受/需修订/待补交/待验证”，不要因一个文件缺失丢弃其他有效成果。补交消息包含具体缺口、原任务 ID 与最新修订号；无需为了小修强制新开对话，可回到原专项。进入新专项时必须给出新的完整首条消息。

## 恢复点与版本规则

优先使用项目已有的 Git 工作流；若独立游戏尚无仓库且适合使用 Git，可在该游戏目录初始化。不要误用父目录里包含其他游戏的仓库，不自动创建远程仓库、推送或公开发布。

每个稳定里程碑保存：

1. 游戏源码和全部运行依赖，包括图片、序列帧、音频、配置及必要依赖锁定文件。
2. 同版本 `PROJECT.md` 和已知限制/测试结论。
3. 可定位的提交/标签或不可覆盖的快照目录。

无 Git 时采用 `releases/v0.1/`、`releases/v0.2/` 等独立快照；复制时排除 `releases/` 自身，防止递归复制。快照建立后从该快照检查入口与资源相对路径，不能依赖当前工作目录里未归档的文件。

版本语义：

| 版本 | 里程碑 |
| --- | --- |
| v0.1 | 核心玩法原型 |
| v0.2 | 角色/美术接入 |
| v0.3 | 动画接入 |
| v0.4 | 音频接入 |
| v0.5 | 教师试玩与修复 |
| v0.6 | 真实学生试玩与迭代 |
| v1.0 | 约定验收通过的正式交付 |

版本是成果标识，不是自动推进许可。测试中的构建可用 `v0.3-candidate-01`；同阶段修复可用 `v0.3.1`，不用覆盖已接受的旧快照。用真实证据决定版本状态；不适用的阶段标记原因，不伪造完成。恢复历史版本前保护当前未保存成果，回退后同步核对事实文件与资源版本。

v1.0 前核对数学正确性、核心规则、目标设备完整流程、资源加载、适用的静音/音频控制、教师试玩、真实学生试玩及关键问题修复。写清已知非阻塞限制。若用户因排期先拿到候选包，可以交付并明确哪些验收尚未完成，不制造无依据的“正式完成”。

## 学生观察最小表

每条观察关联游戏版本和设备；使用匿名编号，无需收集学生姓名。不要要求学生或教师为了填表长时间中断课堂。

| 学生编号/轮次 | 首次操作与是否需提示 | 开始熟练的题次 | 误触/停顿 | 错误后行为与理解 | 数学策略/原话 | 再玩行为 | 教师介入 |
| --- | --- | --- | --- | --- | --- | --- | --- |

把“观察到三次反复点击同一错误答案”与“可能没理解反馈”分别记为事实与解释，再通过调整或复测验证。观察样本有限时保留范围，不把一次试玩当作普遍学习效果结论。

## TeacherLeo Math Game Kit

每款游戏结束时记录真实复用候选：开始页面、按钮、静音/音乐管理、进度、计分、答对动画、错误反馈、重启、全屏适配、触屏点击、题目生成和反馈事件等。

两三款游戏出现重复实现时，再判断接口是否稳定、能否独立验证、抽取是否降低维护成本。题目生成器常含具体知识约束，不强行合成万能生成器；游戏主题和教学规则留在各自项目。

有明确抽取任务时，用至少两个现有场景检查组件适用性，写清输入、输出、默认行为和可配置项，再确定 Kit 版本。不得因更新公共组件自动改变各游戏已锁定的行为；在单款游戏接入和验证后再推广。

单款游戏的事实文件只记录所用 Kit 版本、接入范围和验证结果；Skill 只记录方法，避免三处维护同一份游戏规则。
